apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: rick
  name: redis-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      name: redis-deploy
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis