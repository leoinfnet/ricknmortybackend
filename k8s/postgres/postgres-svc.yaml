apiVersion: v1
kind: Service
metadata:
  namespace: rick
  name: postgres-svc
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres-port
      port: 5432
      targetPort: 5432

